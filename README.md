# argo-curl-plugin

```shell
curl -X POST http://localhost:8080/api/v1/getparams.execute -H "Content-Type: application/json" -d '{"input": {"parameters": {"discoveryURL": "https://api.armosec.io/api/v2/servicediscovery"}}}'
```
